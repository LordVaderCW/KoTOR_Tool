# Embedded Images

This directory contains three PNG assets embedded by the project: two LordVaderCW images and a Miles Sound System logo.

Treat resource names as API because forms may retrieve them by generated or string name. Preserve alpha channel, dimensions/aspect assumptions and redistribution attribution. Add/rename assets through `Resources.resx` and regenerate typed resources where appropriate; verify designer load and runtime scaling at high DPI.
